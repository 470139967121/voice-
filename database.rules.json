{
  "rules": {
    "rooms": {
      "$roomId": {
        "presence": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth != null && auth.uid == $userId",
            ".validate": "newData.isBoolean()"
          }
        },
        "events": {
          "lastEvent": {
            ".read": "auth != null",
            ".write": false
          }
        }
      }
    },
    "conversations": {
      "$convId": {
        "typing": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth != null && auth.uid == $userId",
            ".validate": "newData.isBoolean()"
          }
        },
        "events": {
          "lastEvent": {
            ".read": "auth != null",
            ".write": false
          }
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
