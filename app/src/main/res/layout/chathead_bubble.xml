<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="72dp"
    android:layout_height="72dp">

    <!-- Inner circle with blue border -->
    <FrameLayout
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center"
        android:background="@drawable/chathead_circle_bg">

        <!-- Owner photo (fills inner circle) -->
        <ImageView
            android:id="@+id/ownerPhoto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            android:contentDescription="Room host" />

        <!-- Fallback mic icon -->
        <ImageView
            android:id="@+id/micIcon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@android:drawable/ic_btn_speak_now"
            android:visibility="visible"
            android:contentDescription="Voice room" />

        <!-- Voice wave animation overlay -->
        <com.shyden.shytalk.core.chathead.VoiceWaveView
            android:id="@+id/voiceWaves"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Room closed overlay text (hidden by default) -->
        <TextView
            android:id="@+id/roomClosedText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Room\nClosed"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone" />

    </FrameLayout>

    <!-- Close button at top-right corner -->
    <ImageView
        android:id="@+id/closeButton"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_gravity="top|end"
        android:background="@drawable/chathead_close_button_bg"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:padding="4dp"
        android:scaleType="centerInside"
        android:contentDescription="Close room" />

</FrameLayout>
