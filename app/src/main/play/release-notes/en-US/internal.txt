v0.88.1

subscribe button uses shared login modal + fix sign-in order (#342)
- ## Summary
- - Subscribe button (when unauthenticated) now opens the **shared login
- modal** (`showLoginPromptModal`) instead of building its own duplicate
- inline auth buttons
- - Google/Apple sign-in buttons now call
