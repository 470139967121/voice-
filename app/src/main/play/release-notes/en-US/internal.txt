v0.64.3

local flavor supports physical devices via -PlocalHost (#288)
- ## Summary
- The `local` Android build flavor hardcoded all URLs to `10.0.2.2`
- (Android emulator's host alias), so physical devices showed "Technical
- Difficulties" on launch because API requests failed. This was found
- during PR #284 manual QA on a physical device.
