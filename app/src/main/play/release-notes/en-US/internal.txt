v0.63.3

CRITICAL — room close, gacha coins, Play Store (#248)
- ## Summary
- Three critical fixes:
- ### 1. Room close bug (rooms stay in list after closing)
- **Root cause:** Firestore security rules type mismatch.
- `callerUniqueId()` returns a **Number** from the auth custom claim, but
