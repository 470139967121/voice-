v0.72.0

wire Koin init + update MainViewController (Steps 1.6-1.7) (#318)
- ## Summary
- Updated `MainViewController.kt` to show Koin-initialized
- status with phase progression notes instead of bare placeholder
- `iOSApp.swift` now calls `KoinHelperKt.doInitKoin()` in
- `init()`, loading `viewModelModule + iosPlatformModule` before Compose
