v0.71.0

split Koin DI + fix coin purchase bug (#317)
- ## Summary
- Extract 23 ViewModel bindings from
- Android's `AppKoinModule` into a shared `viewModelModule` in
- `commonMain`. Creates iOS platform module structure
- (`IosPlatformModule`, `KoinHelper`) for Koin initialization from Swift.
