v0.93.4

add GoogleService-Info.plist + plists to Xcode project
- GoogleService-Info.plist was on disk but not referenced in the
- Xcode project file, causing Firebase.configure() to crash with
- SIGABRT on launch. Added as a resource to the build target.
- Claude Opus 4.6 (1M context) <noreply@anthropic.com>
