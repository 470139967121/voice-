v0.93.11

disable code-signing on pod targets (#367)
- disable code-signing on pod targets in Podfile post_install
- The Deploy To Dev / Prod workflows pass `CODE_SIGN_STYLE=Manual` and
- `PROVISIONING_PROFILE_SPECIFIER="ShyTalk App Store Distribution"` as global
- xcodebuild CLI flags. CLI flags override per-target settings AND apply to
- every target in the workspace, including all CocoaPod static-library targets
