v0.55.6

CRITICAL — MainActivity crash on launch (ClassCastException) (#196)
- MainActivity extended ComponentActivity but biometricAuth.setActivity()
- cast it to FragmentActivity, causing an immediate crash on every launch.
- extend AppCompatActivity (which IS a FragmentActivity) instead of
- ComponentActivity. Removes the unsafe cast.
- Claude Opus 4.6 (1M context) <noreply@anthropic.com>
