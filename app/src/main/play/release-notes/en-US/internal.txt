v0.90.0

move AppSettingsScreen to shared/commonMain (iOS parity) (#346)
- add PlatformBackHandler + settings helper functions
- Infrastructure for AppSettingsScreen migration to shared/commonMain:
- wraps Android BackHandler,
-   no-op on iOS/JVM (iOS has no system back button)
- censorEmail(), formatCacheSize(), formatTime()
