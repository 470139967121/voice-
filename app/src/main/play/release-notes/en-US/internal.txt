v0.63.7

release workflow — don't skip on workflow_dispatch (#279)
- Manual workflow_dispatch triggers should always run the release job,
- bypassing the workflow_only skip check (which examines HEAD~1 diff
- and would skip if the latest commit is workflow-only).
- Claude Opus 4.6 (1M context) <noreply@anthropic.com>
