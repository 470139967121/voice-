v0.88.2

roadmap sign-in modal + Firebase config fallback (#343)
- roadmap sign-in modal + Firebase config fallback
- Three fixes for the roadmap page sign-in:
- 1. Sign-in modal no longer closes immediately when clicking Google/Apple
-    buttons. The modal stays open while the async popup processes, then
-    auto-closes via the shytalk-auth-changed event after auth succeeds.
