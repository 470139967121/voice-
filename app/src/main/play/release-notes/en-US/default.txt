v0.94.7

/api/health returns deployed SHA (closes Phase 0 #64) (#397)
- Closes the "deploy succeeded but old pm2 process still running"
- silent-failure class. Without this, a deploy could:
-   1. scp the new tarball
-   2. tar xzf onto the server
-   3. pm2 restart returns 0
