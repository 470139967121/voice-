v0.94.3

dedup rtdbEventsFailed counter in evict-suspended-user (#386)
- For owner-closure rooms, the cascade fires TWO RTDB calls:
- 'room_closed' })
-   2. remove('rooms/<id>')
- Both incremented the same `rtdbEventsFailed` counter, so a room
- 2`
