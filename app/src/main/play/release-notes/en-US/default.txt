v0.63.5

admin panel CSP errors, backup metadata, and banner upload (#252)
- ## Summary
- - Remove invalid `X-Frame-Options` and `frame-ancestors` meta tags
- (browser ignores these in `<meta>`, must be HTTP headers)
- - Add `blob:` to CSP `img-src` directive to fix banner image preview
- blocking
