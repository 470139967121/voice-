v0.59.3

SonarCloud must pass before E2E/Playwright + pre-push hook (#235)
- Pipeline change:
-   lint → backend tests → build → SonarCloud → E2E/Playwright/iOS
-   If SonarCloud quality gate fails, E2E/Playwright/iOS are skipped,
-   saving ~20 minutes of runner time.
- Pre-push hook (.husky/pre-push):
