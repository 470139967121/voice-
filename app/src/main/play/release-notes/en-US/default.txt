v0.88.5

bell auth check + Google account picker (#347)
- 1. Bell icons now check auth state before showing login modal. When
-    authenticated, they open the subscribe modal instead. Exposed
-    shytalkOpenSubscribeModal globally from suggestions-board.js.
- 'select_account' so users can
-    choose which account to use instead of auto-selecting the last one.
