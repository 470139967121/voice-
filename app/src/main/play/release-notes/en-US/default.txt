v0.59.1

PR Gate must require detect-changes success + full branch diff (#221)
- Two issues caused PR #218 to auto-merge without Playwright validation:
- 1. PR Gate treated skipped checks as passing when detect-changes was
-    cancelled. Now explicitly requires detect-changes == success.
- 2. Detect-changes only diffed the latest push against base, not the
-    full branch. A push with only workflow files made it think there
