v0.64.2

add favicon + document FIREBASE_WEB_API_KEY for local dev (#287)
- ## Summary
- Two pre-existing issues found during PR #284 manual QA, bundled as a
- single dev-QoL PR.
- ## 1. Missing favicon (closes #17)
- Every web page emitted a `/favicon.ico` 404 on load because no HTML file
