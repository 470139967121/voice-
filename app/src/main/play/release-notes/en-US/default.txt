v0.73.0

add GitLive Firebase KMP + iOS repository stubs (Phase 2) (#319)
- ## Summary
- Added GitLive Firebase KMP 2.4.0 to `iosMain`
- dependencies (auth, firestore, database, app). Android is unaffected.
- Created 26 stub implementations for ALL
- repository/service interfaces. Registered in `IosPlatformModule` with
