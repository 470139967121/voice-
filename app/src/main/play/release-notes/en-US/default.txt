v0.55.5

iOS compilation errors — BiometricAuth opt-in, CryptoKeyPair types (#194)
- iOS compilation errors + Play Store release notes
- iOS fixes:
- add @ExperimentalForeignApi file-level opt-in
- replace NSString cast with encodeToByteArray()
- use CFTypeRefVar for SecItemCopyMatching
