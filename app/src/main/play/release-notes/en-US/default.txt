v0.94.5

wire admin UI to consume PM partial-failure flags (#392)
- wire admin UI to consume PM partial-failure flags
- Server side (PRs #384, #385) emits `pms: { failed, total }` on every
- admin route where a user-visible PM might be silently dropped, but
- the admin UI was discarding those responses entirely — the new
- signal landed as bytes-on-the-wire that admins never saw, defeating
